// Screen 4 — plan/DASHBOARD.md §Screen 4. Promote to NC, or close as no action.
// Closing is a permanent record with a named reviewer and a rationale, not a deletion.
export default function SignalQueue() {
  return <h1 className="text-lg font-medium">Signal queue</h1>
}
